## 🐍 파이썬 프로그래밍의 기초: 자료형 (Data Types)



### 1\. 숫자형 (Numeric Types)

숫자형은 수학적 연산에 사용되는 값들을 나타냅니다.

|         자료형         |            설명             |     기본 형태 예시     |
| :--------------------: | :-------------------------: | :--------------------: |
|    **`int`** (정수)    |    소수점 이하가 없는 수    |    `num_int = 123`     |
|   **`float`** (실수)   |    소수점 이하가 있는 수    |   `num_float = 3.14`   |
| **`complex`** (복소수) | 실수부와 허수부를 가지는 수 | `num_complex = 1 + 2j` |

#### ✨ 주요 메서드/함수 예시

```python
a = 10
b = 3.14
c = -7

print(abs(c))      # 7 (절댓값)
print(int(b))      # 3 (정수로 변환, 소수점 버림)
print(float(a))    # 10.0 (실수로 변환)
print(round(3.14159, 2)) # 3.14 (소수점 둘째 자리까지 반올림)
```

---

### 2\. 문자열 (String Type: `str`)

문자열은 텍스트 데이터를 저장하는 데 사용되며, 불변(Immutable)의 특징을 가집니다. 즉, 한 번 생성되면 그 내용을 직접 수정할 수 없습니다.

#### ✨ 기본 형태 예시

```python
str1 = 'Hello World'
str2 = "Python Programming"
str3 = """Multi
Line
String"""
```

#### ✨ 주요 메서드 예시

```python
s = "   Hello Python! "

# 대소문자 변환
print(s.upper())     #    HELLO PYTHON!
print(s.lower())     #    hello python!

# 공백 제거
print(s.strip())     # Hello Python!

# 분리 (리스트로 반환)
data = "apple,banana,cherry"
print(data.split(',')) # ['apple', 'banana', 'cherry']

# 대체
s_new = s.replace('Python', 'Java').strip()
print(s_new)         # Hello Java!

# 인덱싱 (위치 찾기)
print(s.find('Python'))  # 8 (P가 시작하는 인덱스)

# f-string (포매팅)
name = 'Alice'
age = 25
print(f"My name is {name} and I am {age} years old.")
# My name is Alice and I am 25 years old.
```

---

### 3\. 리스트 (List Type: `list`)

리스트는 여러 값을 순서대로 저장하며, **변경 가능한(Mutable)** 자료형입니다. with indices 0, 1, 2]

#### ✨ 기본 형태 예시

```python
my_list = [10, "Python", 3.14, True]
empty_list = []
```

#### ✨ 주요 메서드 예시

```python
fruits = ['apple', 'banana', 'cherry']

# 추가 (맨 뒤)
fruits.append('grape')
print(fruits)      # ['apple', 'banana', 'cherry', 'grape']

# 삽입 (특정 위치)
fruits.insert(1, 'orange')
print(fruits)      # ['apple', 'orange', 'banana', 'cherry', 'grape']

# 제거 (값으로)
fruits.remove('banana')
print(fruits)      # ['apple', 'orange', 'cherry', 'grape']

# 제거 (인덱스로 & 값 반환)
popped_fruit = fruits.pop(0) # 'apple'
print(fruits)      # ['orange', 'cherry', 'grape']
print(popped_fruit) # apple

# 정렬 (원본 변경)
nums = [5, 2, 8, 1]
nums.sort()
print(nums)        # [1, 2, 5, 8]
```

---

### 4\. 튜플 (Tuple Type: `tuple`)

튜플은 리스트와 유사하지만, **변경 불가능한(Immutable)** 자료형입니다. 주로 데이터가 변경되지 않아야 할 때 사용됩니다.

#### ✨ 기본 형태 예시

```python
my_tuple = (1, 2, "three", 4.0)
single_item_tuple = (5,) # 요소가 하나일 때는 쉼표(,) 필수
```

#### ✨ 주요 메서드/함수 예시

```python
colors = ('red', 'green', 'blue', 'red')

# 인덱스 찾기
print(colors.index('green')) # 1

# 요소 개수 세기
print(colors.count('red'))   # 2

# 길이 (함수)
print(len(colors))           # 4
```

---

### 5\. 딕셔너리 (Dictionary Type: `dict`)

딕셔너리는 \*\*`키(Key)`\*\*와 \*\*`값(Value)`\*\*의 쌍으로 데이터를 저장하며, **키**를 통해 **값**에 접근합니다.

#### ✨ 기본 형태 예시

```python
person = {
    'name': 'Bob',
    'age': 30,
    'city': 'Seoul'
}
empty_dict = {}
```

#### ✨ 주요 메서드 예시

```python
person = {'name': 'Bob', 'age': 30, 'city': 'Seoul'}

# 값 접근 및 추가/수정
print(person['name']) # Bob
person['age'] = 31    # 값 수정
person['job'] = 'Engineer' # 새 항목 추가

# 키 목록
print(person.keys())   # dict_keys(['name', 'age', 'city', 'job'])

# 값 목록
print(person.values()) # dict_values(['Bob', 31, 'Seoul', 'Engineer'])

# 항목 목록 (키, 값 쌍)
print(person.items())  # dict_items([('name', 'Bob'), ('age', 31), ...])

# 안전하게 값 가져오기
print(person.get('country', 'Unknown')) # Unknown (키가 없어도 오류 발생 X)

# 항목 제거
removed_age = person.pop('age')
print(person) # {'name': 'Bob', 'city': 'Seoul', 'job': 'Engineer'}
```

---

### 6\. 집합 (Set Type: `set`)

집합은 **중복을 허용하지 않고 순서가 없는** 자료형입니다. 주로 수학적 집합 연산에 사용됩니다.

#### ✨ 기본 형태 예시

```python
my_set = {1, 2, 3, 2, 1} # 중복이 제거되어 {1, 2, 3}만 남음
empty_set = set() # 빈 집합을 만들 때는 set() 함수를 사용해야 함
```

#### ✨ 주요 메서드 예시

```python
A = {1, 2, 3, 4}
B = {3, 4, 5, 6}

# 요소 추가
A.add(5)
print(A) # {1, 2, 3, 4, 5}

# 요소 제거
A.remove(1)
# A.remove(10) # 이 경우, 요소가 없어서 KeyError 발생
print(A) # {2, 3, 4, 5}

# 합집합
print(A.union(B))         # {2, 3, 4, 5, 6}
print(A | B)              # {2, 3, 4, 5, 6}

# 교집합
print(A.intersection(B))  # {3, 4, 5}
print(A & B)              # {3, 4, 5}

# 차집합
print(A.difference(B))    # {2}
print(A - B)              # {2}
```

---

### 7\. 불리언 (Boolean Type: `bool`)

참과 거짓을 나타내는 가장 기본적인 논리 자료형입니다.

#### ✨ 기본 형태 예시

```python
is_active = True
is_empty = False
```

#### ✨ 주요 함수 예시

```python
print(bool(1))      # True (0이 아닌 모든 숫자는 True)
print(bool(0))      # False

print(bool("hello")) # True (비어있지 않은 문자열은 True)
print(bool(""))     # False (빈 문자열)

print(bool([1, 2])) # True (비어있지 않은 리스트)
print(bool([]))     # False (빈 리스트)

x = None
print(bool(x))      # False (None은 False로 간주)
```
